# --- core runtime ---
fastapi==0.115.5
uvicorn[standard]==0.32.1
httpx==0.27.2
pydantic==2.9.2
pydantic-settings==2.6.1
python-dotenv==1.0.1

# --- persistence ---
sqlalchemy[asyncio]==2.0.36
alembic==1.14.0
aiosqlite==0.20.0
asyncpg==0.30.0

# --- auth (Clerk JWT verification via JWKS) ---
pyjwt[crypto]==2.10.1
cryptography==44.0.0

# --- AI / triage ---
anthropic==0.42.0

# --- production hardening ---
slowapi==0.1.9          # per-tenant rate limiting
structlog==24.4.0       # JSON logs
sentry-sdk[fastapi]==2.19.2
prometheus-client==0.21.1

# --- dev / tests ---
pytest==8.3.3
pytest-asyncio==0.24.0
ruff==0.8.4
mypy==1.13.0
